---
name: getting-feedback-remote
description: Gathers build results and reviews, returns all the itemized issues and suggestions as structured TOON data.
agent: quality-data-extractor
user_invocable: false
---

# Getting Feedback for a Pull Request (PR)

I would like you to gather all the feedback (workflow failures and review comments),
  that is available for and relevant to the most recent commit
  in the PR specified in the request to run this skill
  (which should be contained in structured TOON data that was generated by the getting-branch-state skill),
  then parse that feedback into the actionable issues and suggestions extracted from it,
  and return that parsed data in a structured unified TOON-formatted output.
I will give you a checklist of Feedback Steps 1 though 4
  and ask you to track your progress through them.
Some Feedback Steps may also have their own checklists and steps,
  which you will be asked to track your progress through, as well.

Many steps will produce and/or consume TOON-formatted data,
  which is a structured data format suited for use by agents.
TOON is similar to JSON and YAML but uses 2-space indents and arrays show length and fields.
Here's a small toy example:

```toon
context:
  task: Our favorite hikes together
  location: Boulder
  season: spring_2025
friends[3]: ana,luis,sam
hikes[3]{id,name,distanceKm,elevationGain,companion,wasSunny}:
  1,Blue Lake Trail,7.5,320,ana,true
  2,Ridge Overlook,9.2,540,luis,false
  3,Wildflower Loop,5.1,180,sam,true
```

GitHub pull requests (PRs) can have feedback from multiple sources:

- Their CI/CD workflows can produce build failures, test failures, lint failures, etc.
- Review comments from multiple potential sources:
  - Human reviewers via GitHub PR reviews.
  - Unresolved comment threads from earlier commits.
  - AI code reviewers (e.g. Claude bot).

That feedback is likely large and unstructured.
To ensure that it is both understandable and actionable,
  we will use the process described below to gather it
  and parse out the actionable issues and suggestions,
  returning them all in a single structured TOON result.

This checklist specifies the top-level Feedback Steps for the gathering and parsing process.
Copy this checklist and track your progress through it:

```
PR Feedback Gathering and Parsing Progress:
[ ] Feedback Step 1: Wait for Workflows to Complete
[ ] Feedback Step 2: Fetch Build Results for Failed Workflows
[ ] Feedback Step 3: Fetch and Parse Review Suggestions
[ ] Feedback Step 4: Build Unified Output and Return Response
```

## Feedback Step 1: Wait for Workflows to Complete

See [Feedback Step 1: Wait for Workflows](steps/feedback_step_1_wait_for_workflows.md) and follow its instructions.

## Feedback Step 2: Fetch Build Issues

See [Feedback Step 2: Fetch Build Issues](steps/feedback_step_2_fetch_build_issues.md) and follow its instructions.

## Feedback Step 3: Fetch and Parse Review Suggestions

See [Feedback Step 3: Fetch Review Suggestions](steps/feedback_step_3_fetch_review_suggestions.md) and follow its instructions.

## Feedback Step 4: Combine and Return Response

See [Feedback Step 4: Combine and Return](steps/feedback_step_4_return_combined_response.md) and follow its instructions.

## Common Mistakes

Not waiting for workflows:
- Problem: Try to fetch logs while workflows still running
- Fix: Always run awaiting-pr-workflow-results first

Fetching logs for passing workflows:
- Problem: Waste time and tokens
- Fix: Only fetch logs for failed workflows

Not providing changed files context:
- Problem: Can't determine if failures are related to changes
- Fix: Include `git diff --name-only` in context for parsing skill

Returning raw data instead of unified TOON:
- Problem: Main context gets overwhelmed
- Fix: Always merge and summarize into unified structure

## Error Handling

If awaiting-pr-workflow-results fails:
- Return status: "blocked"
- Include error message
- Recommendation: Check git/gh setup

If getting-build-results-remote fails:
- Note which workflows couldn't be fetched
- Continue with reviews
- Include partial results

If getting-reviews-remote fails:
- Continue with build results
- Note that review data unavailable
- Include partial results

Always return TOON output, even on partial failure. Main context needs actionable information.
